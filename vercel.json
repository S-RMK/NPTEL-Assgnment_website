{
  "buildCommand": "node -e \"const fs=require('fs'); const cp=require('child_process'); if (fs.existsSync('client/package.json')) process.chdir('client'); cp.execSync('npm run build', {stdio: 'inherit'});\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}